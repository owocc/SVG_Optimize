export interface SvgOptions {
  removeClasses: boolean;
  removeWidth: boolean;
  removeHeight: boolean;
  removeXmlns: boolean;
  previewColor: string;
  applyPreviewColor: boolean;
  outputSizes: string;
}

export type Theme = 'light' | 'dark' | 'system';
